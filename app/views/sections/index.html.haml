%h1 Listing sections

%table
  %tr
    %th Title
    %th Content
    %th
    %th
    %th

  - @sections.each do |section|
    %tr
      %td= section.title
      %td= section.content
      %td= link_to 'Show', [@resume, section]
      %td= link_to 'Edit', edit_resume_section_path(@resume, section)
      %td= link_to 'Destroy', [@resume, section], :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Section', new_resume_section_path(@resume)
